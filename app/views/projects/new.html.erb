<% content_for :title, "Create a new project" %>

<%= simple_form_for @project do |f| %>
    <%= f.input :title, placeholder: "Enter a title", error_prefix: "Project Title" %>
    <%= f.input :description, placeholder: 'Enter a description for your project', error_prefix: "First Name" %>
    <%= f.submit %>
<% end %>
